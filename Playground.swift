import SwiftUI
import PlaygroundSupport

// Este es un playground simplificado para probar la app
struct PlaygroundView: View {
    var body: some View {
        ContentView()
    }
}

PlaygroundPage.current.setLiveView(PlaygroundView())
